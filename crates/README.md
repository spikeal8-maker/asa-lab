# crates/

Rust simulation kernels compiled to WASM (browser) and native binaries (server
golden tests / autograders).

Intentionally empty in the Bootstrap iteration (TASK-BOOT-001). The Rust
toolchain and the first simulation crate are introduced in the Simulation Core
phase (PHASE-6), not during Bootstrap.

# LRN-M0-002 lane bootstrap

This temporary marker creates the documentation-only pull request required by
the parallel-lane control-plane schema. It carries no architecture decision and
will be removed when the execution spec and ADR are committed.

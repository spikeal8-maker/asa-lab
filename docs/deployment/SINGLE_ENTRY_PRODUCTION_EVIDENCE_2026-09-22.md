# Единый вход ASA Lab: проверка production 22 сентября 2026

Это датированный отчёт об установке, а не источник текущего execution state.
Актуальная задача хранится только в `docs/execution/current.yaml`.

## Результат

На существующей Windows-установке запущен проверенный выпуск
`f56a71edb41548932d47154d68fcde87b64402da`, схема БД 159, профиль `production`.
Вход пользователя — `https://asa-lab.ru`. Scratch открывается внутри ASA;
его ресурсы проходят через Web по `/internal/blocks/` на том же origin.
`www.asa-lab.ru` возвращает 308 на основной домен, сохраняя путь и параметры.

Сохранены канонический каталог `F:\Asa-Lab\asa-lab`, Web `127.0.0.1:4610`,
проект Compose `asa-lab-dev` и прежние volumes PostgreSQL/MinIO. Историческое имя
Compose не определяет режим: API работает с `NODE_ENV=production`, dev seeding
выключен, порт API больше не опубликован на хосте.

## Проверки

- [Общие проверки, сборка, PostgreSQL/RLS и вход пользователей](https://github.com/spikeal8-maker/asa-lab/actions/runs/35671116923) — success.
- [Scratch: браузерные сценарии, установка с нуля, повторный PowerShell startup и сохранение/повторное открытие проекта](https://github.com/spikeal8-maker/asa-lab/actions/runs/35671116918) — success.
- [Переносимая установка и контракты Windows/Linux](https://github.com/spikeal8-maker/asa-lab/actions/runs/35671116934) — success.
- [Публикация образов и проверочное восстановление в изолированном CI](https://github.com/spikeal8-maker/asa-lab/actions/runs/35671685474) — success.
- Независимый code review указанного SHA — PASS; локальный `gate:blocks` выполнен с отключённым Nx cache.
- После переключения выполнено 15 HTTP-проверок через публичный HTTPS-вход: health/SHA, портал, конфигурация, документ редактора, скрипты, worker, stock manifest и редирект `www`.
- Повторный `asa_manager.py check` — `NO_CHANGE`, без backup или перезапуска.

В предыдущем кандидате браузер обнаружил абсолютный путь Scratch Storage worker
`/chunks/...`. Исправлен браузерный `publicPath` Storage, добавлена регрессия на
встроенный путь. Проверки ошибок браузера сохранены; ошибочный кандидат не
переключался в эту работающую установку.

## Данные и обслуживание

До переключения создан полный проверенный backup:
`F:\Asa-Lab\asa-lab\backups\20260922T003215Z-b180f326`.
Рабочая БД не восстанавливалась и не мигрировала. PostgreSQL сохранил прежний
image ID; физические volumes не менялись. До и после: 2 аккаунта, 5 проектов,
5 черновиков. Новых пользовательских проектов для проверки не создавалось.

Автообновления включены: воскресенье 03:00–05:00 МСК. Настройки теперь сохраняют
профиль `production`; проверенный запуск вне окна вернул `SKIP`. Перед следующей
сменой выпуска менеджер проверяет установку, готовые образы и полный backup.

FRP работает как Windows-служба `ASAFrpc`: LocalSystem, отложенный автоматический
старт, перезапуск после сбоя через 10/30/60 секунд. Служба `IZOFrpc` не изменялась.
Единственный ASA-прокси направляет публичные домены на Web 4610; отдельного
Scratch-прокси нет. Ключ ASA-службы сохранён в приватной конфигурации, без
зависимости от временного окружения ручного запуска.

Docker Desktop настроен на запуск при входе пользователя в Windows;
`com.docker.service` — Automatic. Работа Docker Desktop до интерактивного входа
в Windows этим переключением не проверялась и не заявляется.

Приватная копия настроек FRP находится в
`F:\Asa-Lab\host-recovery\20260922-asa-frp`; исполняемые файлы в неё не входят.
Backup и FRP TOML содержат секреты: они не публиковались в GitHub.

Локальное подтверждение публичных проверок и количества данных:
`F:\Asa-Lab\single-entry-production-verification.json`.
Проверки сохранения/открытия редактора выполнены в изолированном CI; проекты
владельца на рабочем сайте не редактировались. Owner acceptance остаётся
отдельной пользовательской проверкой и не подменяется этим отчётом.

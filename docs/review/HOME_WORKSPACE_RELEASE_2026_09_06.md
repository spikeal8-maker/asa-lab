# Главная — проверяемая поставка 2026-09-06

Протокол этого пакета, не источник активного состояния направлений.
Продолжает [первый локальный прогон](HOME_WORKSPACE_2026_09_06.md).
Владелец согласовал объединение с работой Electronics/3D и публикацию на сайте.

## Состав

Локальный неопубликованный пакет перенесён поверх `5400d919504397a6b88a0dac20c3a3c053cbb9d3`.
Сохранены шесть новых upstream-коммитов Electronics/3D, включая solverRevision v18.
Опубликованная история не переписывалась; чужие assets, данные и инфраструктура
не удалялись. Новые зависимости и миграции в этом уточнении не добавлены.

- Фирменная шапка, аватар и быстрые настройки; «Проекты» и «Знания».
- Мобильное меню вместо горизонтальной ленты навигации; создание рядом с Главной.
- До 10 собственных работ каждой среды: четыре карточки на компьютере,
  несколько и край следующей на телефоне. Нативная прокрутка без мобильной полосы.
- Прямое создание с серверным именем, без повторного проекта при потере ответа.
- Полки публичных проектов и курсов; закрытые материалы не включаются.
- Точки 16 px: MAX у аккаунта, задания/проверка у меню. Три мягких цикла,
  reduced-motion выключает анимацию. Никаких вымышленных оценок/событий игр.
- Помощь, ВКонтакте и бот MAX доступны из меню. Токены не менялись.

## Контракт знаний

`GET /api/gallery/knowledge?offset=0&limit=10` возвращает до 10 элементов
`{id,title,summary,authorName,publishedAt,lessonCount}` и `nextOffset`.
Без `limit=10` размер страницы 24; некорректный offset заменяется на 0.
SQL выдаёт только `kind=course AND visibility=public` из существующего
`shared_catalogue`, с устойчивой сортировкой по времени и ID.

`GET /api/gallery/knowledge/:courseId` повторно проверяет опубликованную
видимость в одном SQL-снимке и выдаёт безопасную learner-проекцию опубликованной
версии: разделы и материалы без teacher-only assignment payload. Удалённая
публикация, private и чужой school/shared курс недоступны по прямому URL.
Невалидный ID — 400, отсутствие сессии — 401, недоступный курс — 404.
Переиспользованы существующие session/StudentSeat checks, grants и RLS.

Это каталог для вошедших пользователей, не новая гостевая платформа.
Существующий shared catalogue не включает собственные курсы автора; для
управления ими остаётся «Курсы и задания». Самостоятельная запись на курс,
статьи/видео как отдельные публикации и универсальный inbox — последующие этапы.

## Проверки до коммита

- `pnpm gate:code` — PASS; lint 25, typecheck 25 + 14 build dependencies,
  build 25 выполнены заново, `NX_SKIP_NX_CACHE=true`. Compose реально проверен.
  Entry JS/CSS 422314 / 600000 B, бюджеты редакторов не ослаблены.
- `pnpm gate:data` — PASS: 102 migration files, 220 test files / 1723 tests,
  затем отдельные 15 RLS. Настоящий PostgreSQL 17.7, только временная `_test` БД.
- `pnpm test:project-slice` — PASS 75/75 на настоящем PostgreSQL.
- `pnpm test:creator-portal` — PASS 37/37.
- `pnpm gate:governance` и `git diff --check` — PASS.
- Браузер: `pnpm e2e:creator-portal --workers=1` — PASS 9/9 на окончательной
  production-сборке (1.8 min при одном CPU тестового контейнера).
- `pnpm e2e:games --workers=1` — PASS 5/5, существующая mock-API fixture.

Первый общий Vitest-прогон прерван OOM тестового контейнера (ERR_IPC_CHANNEL_CLOSED),
не засчитан. Повтор той же команды после ограничения только временного runner
до одного CPU завершился успешно. Production readiness остался ready на
прежнем SHA. Рабочая база не использовалась для тестов.

Проверены 320/360/390/430/768/1024/1440 px, открытие/закрытие drawer,
фокус и Escape, avatar, отсутствие перекрытий и горизонтального overflow,
44 px hit target при 32 px видимой кнопке, reduced-motion, источник счётчика,
20 возвратов из редакторов, потеря POST-ответа/reload, 20 аккаунтов одного IP,
архив/корзина/восстановление, публичный курс/private/revocation.
Fixtures и скриншоты содержат только синтетические данные из временной БД.

## Границы поставки

TASK: завершить Главную и объединить готовые Electronics/3D изменения.
ISSUE: направление projects / Issue 37; отдельная Issue не создавалась.
STATUS: локальные проверки пакета; push, CI точного SHA и deployment — отдельные receipts.
VISIBLE_RESULT: компактная Главная, полки, меню и работающие ссылки знаний.
USER_FLOW: вход → Главная → Создать / карточка → редактор → Back; Знания → курс.
PORTS: постоянные 4610/4611 не изменены; QA только 127.0.0.1:4612.
DEMO_URLS: https://asa-lab.ru/#/home после защищённого deployment.
SCREENSHOTS: e2e/artifacts/owner-preview/home-release/ и home-workspace/after/.
MAP_NODES_CHANGED: нет; RLS/tenant модель и форматы документов сохранены.
WORKING_TREE: только перечисленные файлы пакета, без owner assets/секретов/дампов.
NEXT_ALLOWED_TASK: финальные receipts → обычный push → CI точного SHA → guarded updater → owner acceptance.

Updater обязан проверить рабочий каталог PostgreSQL, CI, создать и проверить
backup, сохранить rollback images, проверить одинаковый SHA Web/API и схему.
CI, deployment и принятие владельцем не выводятся из локальных PASS.

## Уточнение плотности после снимка владельца

Пакет `410af22b0322138fcfaa46f9eb7f405f8c8f563d` опубликован в main.
Его основной workflow `34030403925` завершился success, но deployment ещё
не выполнялся: владелец попросил убрать избыточное свободное пространство.

- Отступ между разделами: 10 px на телефоне, 14 px на компьютере.
  Между заголовком и полкой — 2 px; уменьшены поля карточек и пустых состояний.
- Вместо отдельных строк «Все модели/Все схемы» работает заголовок раздела.
  Кнопка создания остаётся справа; стрелки прокрутки на компьютере перенесены
  в ту же строку. На телефоне полосы прокрутки и отдельного ряда стрелок нет.
- Размер основного текста не уменьшен. Заголовок-ссылка и создание имеют
  зону нажатия 44 px; видимый фон создания остаётся высотой 32 px.
- Добавлена браузерная регрессия: отступы не растут, заголовки не пересекаются
  с созданием на 320–1440 px, заполненная полка сохраняет край следующей карточки.
- Обновлён только входной helper `e2e/three-d-m0.spec.ts`: новое меню создания,
  автоматическое имя и переименование через реальный PATCH. Проверки модели,
  Worker, экспорта и сохранения не ослаблены. Старый helper ожидал удалённый
  диалог; поэтому прежний 3D workflow остановлен и не считается успешным.

Снимки после уплотнения сохраняются в `home-workspace/after/` на синтетических
данных отдельной временной БД. Новых миграций, зависимостей, смены токенов и
изменений редакторов в уточнении нет. Финальный SHA/CI/deployment фиксируются
отдельно после завершения проверок.

Локальный полный 3D-прогон также выявил гонку старого `dismissNotice`: toast
исчезал автоматически между `isVisible` и `click`, после чего тест ждал до
общего timeout. Helper теперь ограничивает ожидание клика и принимает только
подтверждённое исчезновение уведомления; если кнопка остаётся видна, ошибка
не подавляется. Это исправление теста, не изменение поведения редактора.

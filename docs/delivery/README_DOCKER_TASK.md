For the owner-approved Windows 11 / WSL2 / Linux Docker bootstrap, read `docs/delivery/DOCKER_LINUX_EXECUTION_OVERLAY.yaml` and Issue #69. The implementation branch is `assistant/docker-linux-bootstrap` based on `assistant/chess-online-core`. This overlay exists only to bootstrap the branch; the branch must update the canonical Execution Manifest and Project Map before infrastructure implementation proceeds.

# modules/

Subject modules connected through the versioned Module SDK (electronics first,
then 3D, robotics, chess, drawing).

Intentionally empty in the Bootstrap iteration (TASK-BOOT-001). Modules never
receive direct access to Classroom Core tables and never import core internals.

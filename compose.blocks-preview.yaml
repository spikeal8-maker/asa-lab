# Compatibility overlay. Scratch is included in compose.yaml for every normal installation.
services:
  scratch:
    build:
      args:
        ASA_BLOCKS_PARENT_ORIGIN: ${ASA_BLOCKS_PARENT_ORIGIN:-http://127.0.0.1:${ASA_WEB_PORT:-4610}}
